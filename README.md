# tensor_toolkit
A portable (hopefully) tiny tensor header library for calculations in physics.

## Dependencies
0

## Standard installation
```
git clone github.com/cmhamel/tensor_toolkit.git
cd tensor_toolkit
mkdir build && cd build
cmake ..
make
make install
```

## Installation with enzyme